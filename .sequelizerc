const { resolve } = require("node:path");

module.exports = {
    config: resolve(__dirname, "src", "infrastructure", "config", "database.js"),
    "models-path": resolve(__dirname, "src", "infrastructure", "db", "models"),
    "migrations-path": resolve(__dirname, "src", "infrastructure", "db", "migrations"),
};
